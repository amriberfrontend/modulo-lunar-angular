import { ExtendedFieldText } from './extended-field-text';

describe('ExtendedFieldText', () => {
  it('should create an instance', () => {
    expect(new ExtendedFieldText()).toBeTruthy();
  });
});
